source "https://rubygems.org"

# For local development with Jekyll 4.x
gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.8"

# If deploying via GitHub Pages (classic build), swap the two lines above for:
# gem "github-pages", group: :jekyll_plugins
